很棒！现在有的是前端框架，但是实际游戏逻辑完全没有写。

我们先完成第一个模块，创世纪 (Genesis) - 初始化生成，这是游戏开始时的“重计算”环节。

首先，在用户输入世界观和人物身份后，进行第一层的安全检查。首先检查该信息是否包含hack信息，如果包含则重新生成。若该信息是否在中国环境下被视为敏感信息，则也要求用户重新生成。

然后，进行效用检查。即，检查输入的信息是否是一个合法的世界观；输入的信息是否是一个合法的角色信息。这里的不合法可能包括内容并非一个世界观或角色等等。

最后，进行扩展检查。即，检查输入的信息是否包含“指定数据信息”。用户可以描述世界观，但不能指定自己的任务；用户可以描述玩家角色的背景、身份、特长，但不能直接指定某属性的数值。如果违反，弹出提示，prompt包含强指向性信息，将被弱化。prompt只能为粗略描述，不可以是“武力值为9”或“最终目的是找到打到10点物理”等信息。但“希望找寻自己的身世”之类的描述是被允许的。

每层检查都要加入用户的加载等待，不能干等着，起码有动画，转圈、渐变啥的都行。并且检查要输出结果，安全检查通过、效用检查通过、扩展检查通过这种。

而后，保存用户的世界观信息和身份信息。

- 世界构建器 (World Builder Agent):

  - 输入: 用户选的属性（高魔/低魔等）、世界描述、角色描述。

  - 功能: 这是一个结构化输出的 LLM 调用。它不只是写作文，而是要输出 JSON 格式的数据。

  - 输出内容:
    - `world_setting`: 详细的世界观补全。
    - `factions`: 4 个主要势力的简单描述。
    - `map`:地图，大概8~12个位置。
    - `faction_1`:势力1的详细设定。（背景，势力范围，冲突，能力，组织架构等等）
    - `faction_1`:势力2的详细设定。
    - `faction_1`:势力3的详细设定。
    - `faction_1`:势力4的详细设定。
    - `stats_allocation`: 根据描述自动分配的初始人物属性（属性为力量、敏捷、智力、魅力，总和为15）。
    - `inventory`: 初始道具列表。
    - `Power_level`:一段简单的描述，确定当前世界的魔法、科技、武力水平，用一些例子。
    - `pic_style`:几个词，表示绘画的资产风格。

- 身份与剧情编排器 (Multi-Agent System):

  - Agent A (Narrator): 负责根据世界观，为主角编写一个“合理的出身故事”。传入的信息为世界观的补全、4 个主要势力的简单描述、人物信息、Power_level。

  - Agent B (Quest Master): 负责规划总体的任务线（Main Questline）。它会生成一个包含 5-8 个关键节点的列表，确立最终的“势力目标”。这个任务线应该呈完整的线性流程。传入的信息为世界观的补全、4 个主要势力的详细设定、map、人物信息、Power_level。
    - 返回结构为json，包含
      - 'task_num'
      - 'task_{i}'，代表第i个任务的简略描述，约一小段
      - 'final_goal'
  - Agent C (Skill Master): 负责规划道具或技能。它会生成一个json，包含skill和item。传入的信息为世界观的补全、4 个主要势力的简单描述、人物信息、inventory、Power_level。

- 资产生成器 (Asset Gen):

  - 头像生成: 调用 gpt-image-1.5，根据生成的“人物外貌描述”生成一张符合pic_style的头像。

  - 地图生成: 调用gpt-image-1.5，生成一张简洁的、有文字说明的符合pic_style的地图。

然后，得到上述信息后，进入游戏流程。其他的部分先不管，但是目前已经获取的信息（玩家信息，地图信息）都要展示出来，并且在debug界面（/debug）打印完整的世界构建器的json、身份与剧情编排器的json们、资产生成器的图片们。