现在生成好了创世纪信息；但是，游戏界面显示的信息却不对。你一共需要做三个显示修改和一个功能修改兼显示修改。

【显示修改1】首先，进入游戏时，将玩家身份的白色字体的内容设置为玩家输入的用户描述，下面的灰色字体是origin_story的内容不变；

将故事背景的白色字体设置为world_setting的内容，下面的灰色字体是Power_level的内容。

【显示修改2】其次，点击地图后，上面是地图生成的资产图片，下面是world_setting的描述文字。

【显示修改3】而后，下方的区域是游戏区域。首先，不显示系统任务线，这是后续的prompt；其次，在这里规划一个当前任务的块（类似于之前的系统任务线的块），这里面包含当前的信息。之后可能类似于对方发消息的对话一样持续更新信息。

【功能修改兼显示修改】最后，创世纪结束时，调用TASK_GENE Agent，生成一个任务。这个agent描述一下，传入世界world_setting、origin_story、Power_level、task_desp(任务线里的第一个任务)生成第一个任务的详细说明，调用高质量LLM，返回json，包括

- world_desp_and_user_desp，一段中等长度的描述，包含world_setting、origin_story、Power_level的描述文本，特别是在当前任务下的描述文本
- goal，即玩家做到了什么可以算作任务完成，需要是完整的详细一点的判定条件
- task_desp，即任务的详细描述（对玩家的描述，不能用玩家不应该知道的内容，包含玩家视角下的目标粗略描述）
- task_detail，即任务的详细描述（系统描述，全视角）
- firs_opt，即可能当前任务可能做的第一个事情
- seco_opt，即可能当前任务可能做的第二个事情
- thir_opt，即可能当前任务可能做的第三个事情

然后，在这里显示任务的玩家描述（不显示goal）；然后下面是操作，把原先的缺省文字替换成这三个opt的内容；保留输入自己的行动的部分。

注意！在创世纪成功、任务生成之前，在这个块里做显示【任务推演中】的加载，要有动画，和之前的加载类似。