修改创世纪逻辑与prompt。

- 首先，安全检查部分不需要返回处理后的玩家文本了，只需要返回check。并且，玩家的原始文本需要被保存，之后使用。只要安全检查通过，就进入创世纪等待界面。
- 在这个等待界面里，第一件事是调用优化agent。这是一个你需要新增的agent，使用高质量文本LLM，优化整理玩家的描述；需要过滤掉过于精确的描述，统一人称代词或描述，要有故事感。使用玩家的原始文本作为worldDesc和charDesc的输入，输出过滤后的文本（说白了就是把整理弄一个新的agent），而后，使用优化agent的输出代替原有的sanitizedInput。
- 然后，是继续的其他的agent的流程。不过原有的sanitizedInput会换成优化agent的输出而已。